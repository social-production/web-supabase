# Edge Functions (optional)

Use only when the Supabase JS client cannot cleanly satisfy an `AppAdapter` method
(for example complex feed ranking or multi-step governance). Prefer SQL RPC / RLS first.
