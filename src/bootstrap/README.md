# `bootstrap`

Viewer bootstrap + unread counts (+ onboarding payload).

Frontend driver counterpart:

`../web/src/lib/api/drivers/supabase/domains/bootstrap.ts`

Replace this README with real SQL / RPC / Edge Function notes as implementation lands.
