# `feeds`

Paginated public / home / personal / region / scope / user feeds and map markers.

Frontend driver counterpart:

`../web/src/lib/api/drivers/supabase/domains/feeds.ts`

Replace this README with real SQL / RPC / Edge Function notes as implementation lands.
