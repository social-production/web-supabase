# `auth`

Sign-in / sign-up / sign-out and session restore semantics for the frontend SessionTransport.

Frontend driver counterpart:

`../web/src/lib/api/drivers/supabase/domains/auth.ts`

Replace this README with real SQL / RPC / Edge Function notes as implementation lands.
