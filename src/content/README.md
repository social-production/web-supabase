# `content`

Threads, posts, comments, votes, reports. Wire explicit governance entity types.

Frontend driver counterpart:

`../web/src/lib/api/drivers/supabase/domains/content.ts`

Replace this README with real SQL / RPC / Edge Function notes as implementation lands.
